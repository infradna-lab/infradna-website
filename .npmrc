# @tokens-studio/sd-transforms가 style-dictionary@^5를 peer로 요구하나
# 이 저장소는 4.4.0에 고정돼 있어, 플래그 없는 npm install은 ERESOLVE로 실패한다.
#
# Vercel은 서버리스 함수(api/)를 번들할 때 프로젝트의 Install Command를 무시하고
# 맨 `npm install`을 실행하므로, 대시보드 설정만으로는 부족하다. .npmrc는 모든
# npm 호출에 적용되므로 그 단계까지 덮는다.
legacy-peer-deps=true
